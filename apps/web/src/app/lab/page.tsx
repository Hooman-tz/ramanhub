import { SectionPlaceholder } from "~/components/section-placeholder";

export const metadata = { title: "Lab · Spectra Insight" };

export default function LabPage() {
  return (
    <SectionPlaceholder
      title="My Lab"
      blurb="SpectraBase, the public Commons, the processing pipeline and the analysis toolbox in one workspace. Coming online later in the design port."
    />
  );
}
