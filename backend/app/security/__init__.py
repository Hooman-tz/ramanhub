"""Security-hardening helpers (Module 5): content-based upload validation,
resource-limited parsing, etc. — kept in their own package so they're
independently testable and reusable across routers.
"""
