VITE_API_BASE_URL=http://localhost:8000

# Optional. "owner/repo" on GitHub — powers the "Report a bug" button's
# deep link into a pre-filled New Issue form. Leave blank to hide the button
# (e.g. before the repo has a GitHub remote).
VITE_GITHUB_REPO=
